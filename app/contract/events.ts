import type { HookableEventData } from "blitz-saas/contract/events"
export interface EventsContract extends HookableEventData {
  // Add other app events here. The base hookable events are already registered
}

import { resolver } from "blitz"
import * as z from "zod"
import { {{plural name}}Repository } from "../infra/{{plural name}}Repository"

const Create{{titleCase name}}Schema = z.object({

})

export default resolver.pipe(resolver.zod(Create{{titleCase name}}Schema), async (input) => {
  const {{name}} = await {{plural name}}Repository.create{{titleCase name}}({data: input})
  return {{name}}
})

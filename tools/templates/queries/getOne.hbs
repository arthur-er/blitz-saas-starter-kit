import { resolver } from "blitz"
import * as z from "zod"
import { {{plural name}}Repository } from "../infra/{{plural name}}Repository"
import { Id } from "app/core/domain/valueObjects/id"

const {{name}}FindSchema = z.object({
  id: z.string().uuid(),
})

export default resolver.pipe(resolver.zod({{name}}FindSchema), async (input) => {
  const id = new Id(input.id)
  const {{name}} = await {{plural name}}Repository.get{{titleCase name}}(id)

  return {{name}}
})
